// Transparent mixins: a declaration whose property names a mixin calls it.
border-radius(n = 5px)
  -webkit-border-radius n
  border-radius n

size(w, h = w)
  width w
  height h

.card
  border-radius 3px
  size 100px
